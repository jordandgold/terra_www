{{> global/header}}

	{{> global/side-nav}}

	<div class="main-content">
		<div class="page">

			{{> component/documentation-hero }}
			
			{{{ body }}}

			{{> component/media-queries }}

		</div>
		{{> global/footer}}
	</div>

{{> global/page-bottom}}